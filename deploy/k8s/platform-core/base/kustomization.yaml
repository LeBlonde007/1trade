apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# platform-core base. The platform-core-migrations ConfigMap and the shared `platform-auth` Secret
# are created out-of-band (Tilt / `kubectl` locally; SOPS/Vault in prod) — not committed here.
resources:
  - configmap.yaml
  - service.yaml
  - deployment.yaml

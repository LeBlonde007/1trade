apiVersion: v1
kind: Service
metadata:
  name: inference-runtime
  labels: { app: inference-runtime }
spec:
  selector: { app: inference-runtime }
  ports:
    - { name: http, port: 8000, targetPort: 8000 }

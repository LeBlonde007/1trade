apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Local base = the CPU stub runtime (no GPU). Production GPU runtime: ../runtime-gpu.yaml.
resources:
  - service.yaml
  - deployment.yaml
